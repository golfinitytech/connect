diff --git a/node_modules/@react-navigation/bottom-tabs/lib/module/views/BottomTabBar.js b/node_modules/@react-navigation/bottom-tabs/lib/module/views/BottomTabBar.js
index bb2d9c7..c46ec04 100644
--- a/node_modules/@react-navigation/bottom-tabs/lib/module/views/BottomTabBar.js
+++ b/node_modules/@react-navigation/bottom-tabs/lib/module/views/BottomTabBar.js
@@ -251,12 +251,14 @@ export function BottomTabBar({
       paddingBottom: tabBarPosition === 'bottom' ? insets.bottom : 0,
       paddingTop: tabBarPosition === 'top' ? insets.top : 0,
       paddingHorizontal: Math.max(insets.left, insets.right)
-    }], tabBarStyle],
-    pointerEvents: isTabBarHidden ? 'none' : 'auto',
+    }], tabBarStyle, {
+      pointerEvents: isTabBarHidden ? 'none' : 'auto'
+    }],
     onLayout: sidebar ? undefined : handleLayout,
     children: [/*#__PURE__*/_jsx(View, {
-      pointerEvents: "none",
-      style: StyleSheet.absoluteFill,
+      style: [StyleSheet.absoluteFill, {
+        pointerEvents: "none"
+      }],
       children: tabBarBackgroundElement
     }), /*#__PURE__*/_jsx(View, {
       role: "tablist",
diff --git a/node_modules/@react-navigation/bottom-tabs/src/views/BottomTabBar.tsx b/node_modules/@react-navigation/bottom-tabs/src/views/BottomTabBar.tsx
index 00b843d..840e3b2 100644
--- a/node_modules/@react-navigation/bottom-tabs/src/views/BottomTabBar.tsx
+++ b/node_modules/@react-navigation/bottom-tabs/src/views/BottomTabBar.tsx
@@ -319,19 +319,15 @@ export function BottomTabBar({
           : tabBarPosition === 'right'
             ? styles.end
             : styles.bottom,
-        (
-          Platform.OS === 'web'
-            ? tabBarPosition === 'right'
-            : (direction === 'rtl' && tabBarPosition === 'left') ||
-              (direction !== 'rtl' && tabBarPosition === 'right')
-        )
+        (Platform.OS === 'web'
+          ? tabBarPosition === 'right'
+          : (direction === 'rtl' && tabBarPosition === 'left') ||
+            (direction !== 'rtl' && tabBarPosition === 'right'))
           ? { borderLeftWidth: StyleSheet.hairlineWidth }
-          : (
-                Platform.OS === 'web'
-                  ? tabBarPosition === 'left'
-                  : (direction === 'rtl' && tabBarPosition === 'right') ||
-                    (direction !== 'rtl' && tabBarPosition === 'left')
-              )
+          : (Platform.OS === 'web'
+              ? tabBarPosition === 'left'
+              : (direction === 'rtl' && tabBarPosition === 'right') ||
+                (direction !== 'rtl' && tabBarPosition === 'left'))
             ? { borderRightWidth: StyleSheet.hairlineWidth }
             : tabBarPosition === 'top'
               ? { borderBottomWidth: StyleSheet.hairlineWidth }
@@ -380,11 +376,11 @@ export function BottomTabBar({
               },
             ],
         tabBarStyle,
+        { pointerEvents: isTabBarHidden ? 'none' : 'auto' },
       ]}
-      pointerEvents={isTabBarHidden ? 'none' : 'auto'}
       onLayout={sidebar ? undefined : handleLayout}
     >
-      <View pointerEvents="none" style={StyleSheet.absoluteFill}>
+      <View style={[StyleSheet.absoluteFill, { pointerEvents: 'none' }]}>
         {tabBarBackgroundElement}
       </View>
       <View
