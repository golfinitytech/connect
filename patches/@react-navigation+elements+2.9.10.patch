diff --git a/node_modules/@react-navigation/elements/lib/module/Header/Header.js b/node_modules/@react-navigation/elements/lib/module/Header/Header.js
index db27170..51853fe 100644
--- a/node_modules/@react-navigation/elements/lib/module/Header/Header.js
+++ b/node_modules/@react-navigation/elements/lib/module/Header/Header.js
@@ -244,42 +244,47 @@ export function Header(props) {
     ...props
   }) : customTitle;
   return /*#__PURE__*/_jsxs(Animated.View, {
-    pointerEvents: "box-none",
     style: [{
       height,
       minHeight,
       maxHeight,
       opacity,
       transform
+    }, {
+      pointerEvents: "box-none"
     }],
     children: [/*#__PURE__*/_jsx(Animated.View, {
-      pointerEvents: "box-none",
-      style: [StyleSheet.absoluteFill, backgroundContainerStyle],
+      style: [StyleSheet.absoluteFill, backgroundContainerStyle, {
+        pointerEvents: "box-none"
+      }],
       children: headerBackground ? headerBackground({
         style: backgroundStyle
       }) : /*#__PURE__*/_jsx(HeaderBackground, {
-        pointerEvents:
-        // Allow touch through the header when background color is transparent
-        headerTransparent && (backgroundStyle.backgroundColor === 'transparent' || Color(backgroundStyle.backgroundColor).alpha() === 0) ? 'none' : 'auto',
-        style: backgroundStyle
+        style: [backgroundStyle, {
+          pointerEvents:
+          // Allow touch through the header when background color is transparent
+          headerTransparent && (backgroundStyle.backgroundColor === 'transparent' || Color(backgroundStyle.backgroundColor).alpha() === 0) ? 'none' : 'auto'
+        }]
       })
     }), /*#__PURE__*/_jsx(View, {
-      pointerEvents: "none",
-      style: {
+      style: [{
         height: headerStatusBarHeight
-      }
+      }, {
+        pointerEvents: "none"
+      }]
     }), /*#__PURE__*/_jsxs(View, {
-      pointerEvents: "box-none",
-      style: [styles.content, Platform.OS === 'ios' && frame.width >= IPAD_MINI_MEDIUM_WIDTH ? styles.large : null],
+      style: [styles.content, Platform.OS === 'ios' && frame.width >= IPAD_MINI_MEDIUM_WIDTH ? styles.large : null, {
+        pointerEvents: "box-none"
+      }],
       children: [/*#__PURE__*/_jsx(Animated.View, {
-        pointerEvents: "box-none",
         style: [styles.start, !searchBarVisible && headerTitleAlign === 'center' && styles.expand, {
           marginStart: insets.left
-        }, leftContainerStyle],
+        }, leftContainerStyle, {
+          pointerEvents: "box-none"
+        }],
         children: leftButton
       }), Platform.OS === 'ios' || !searchBarVisible ? /*#__PURE__*/_jsxs(_Fragment, {
         children: [/*#__PURE__*/_jsx(Animated.View, {
-          pointerEvents: "box-none",
           style: [styles.title, {
             // Avoid the title from going offscreen or overlapping buttons
             maxWidth: headerTitleAlign === 'center' ? layout.width - ((leftButton ? headerBackButtonDisplayMode !== 'minimal' ? 80 : 32 : 16) + (rightButton || headerSearchBarOptions ? 16 : 0) + Math.max(insets.left, insets.right)) * 2 : layout.width - ((leftButton ? 52 : 16) + (rightButton || headerSearchBarOptions ? 52 : 16) + insets.left - insets.right)
@@ -287,7 +292,9 @@ export function Header(props) {
             marginStart: 4
           } : {
             marginHorizontal: 16
-          }, titleContainerStyle],
+          }, titleContainerStyle, {
+            pointerEvents: "box-none"
+          }],
           children: headerTitle({
             children: title,
             allowFontScaling: titleAllowFontScaling,
@@ -296,10 +303,11 @@ export function Header(props) {
             style: titleStyle
           })
         }), /*#__PURE__*/_jsxs(Animated.View, {
-          pointerEvents: "box-none",
           style: [styles.end, styles.expand, {
             marginEnd: insets.right
-          }, rightContainerStyle],
+          }, rightContainerStyle, {
+            pointerEvents: "box-none"
+          }],
           children: [rightButton, headerSearchBarOptions ? /*#__PURE__*/_jsx(HeaderButton, {
             tintColor: iconTintColor,
             pressColor: headerPressColor,
diff --git a/node_modules/@react-navigation/elements/lib/module/Header/HeaderSearchBar.js b/node_modules/@react-navigation/elements/lib/module/Header/HeaderSearchBar.js
index c60c74f..b6f9e0f 100644
--- a/node_modules/@react-navigation/elements/lib/module/Header/HeaderSearchBar.js
+++ b/node_modules/@react-navigation/elements/lib/module/Header/HeaderSearchBar.js
@@ -125,12 +125,13 @@ function HeaderSearchBarInternal({
   }
   const textColor = tintColor ?? colors.text;
   return /*#__PURE__*/_jsxs(Animated.View, {
-    pointerEvents: visible ? 'auto' : 'none',
     "aria-live": "polite",
     "aria-hidden": !visible,
     style: [styles.container, {
       opacity: visibleAnim
-    }, style],
+    }, style, {
+      pointerEvents: visible ? 'auto' : 'none'
+    }],
     children: [/*#__PURE__*/_jsxs(View, {
       style: styles.searchbarContainer,
       children: [/*#__PURE__*/_jsx(HeaderIcon, {
diff --git a/node_modules/@react-navigation/elements/lib/module/ResourceSavingView.js b/node_modules/@react-navigation/elements/lib/module/ResourceSavingView.js
index eb063ab..b884610 100644
--- a/node_modules/@react-navigation/elements/lib/module/ResourceSavingView.js
+++ b/node_modules/@react-navigation/elements/lib/module/ResourceSavingView.js
@@ -18,25 +18,28 @@ export function ResourceSavingView({
       hidden: !visible,
       style: [{
         display: visible ? 'flex' : 'none'
-      }, styles.container, style],
-      pointerEvents: visible ? 'auto' : 'none',
+      }, styles.container, style, {
+        pointerEvents: visible ? 'auto' : 'none'
+      }],
       ...rest,
       children: children
     });
   }
   return /*#__PURE__*/_jsx(View, {
-    style: [styles.container, style]
+    style: [styles.container, style, {
+      pointerEvents: visible ? 'auto' : 'none'
+    }]
     // box-none doesn't seem to work properly on Android
     ,
-    pointerEvents: visible ? 'auto' : 'none',
     children: /*#__PURE__*/_jsx(View, {
       collapsable: false,
       removeClippedSubviews:
       // On iOS & macOS, set removeClippedSubviews to true only when not focused
       // This is an workaround for a bug where the clipped view never re-appears
       Platform.OS === 'ios' || Platform.OS === 'macos' ? !visible : true,
-      pointerEvents: visible ? 'auto' : 'none',
-      style: visible ? styles.attached : styles.detached,
+      style: [visible ? styles.attached : styles.detached, {
+        pointerEvents: visible ? 'auto' : 'none'
+      }],
       children: children
     })
   });
diff --git a/node_modules/@react-navigation/elements/lib/module/Screen.js b/node_modules/@react-navigation/elements/lib/module/Screen.js
index 5a7086e..965c336 100644
--- a/node_modules/@react-navigation/elements/lib/module/Screen.js
+++ b/node_modules/@react-navigation/elements/lib/module/Screen.js
@@ -46,14 +46,15 @@ export function Screen(props) {
       navigation: navigation,
       children: /*#__PURE__*/_jsx(View, {
         ref: headerRef,
-        pointerEvents: "box-none",
         onLayout: e => {
           const {
             height
           } = e.nativeEvent.layout;
           setHeaderHeight(height);
         },
-        style: [styles.header, headerTransparent ? styles.absolute : null],
+        style: [styles.header, headerTransparent ? styles.absolute : null, {
+          pointerEvents: "box-none"
+        }],
         children: header
       })
     }) : null, /*#__PURE__*/_jsx(View, {
diff --git a/node_modules/@react-navigation/elements/src/Header/Header.tsx b/node_modules/@react-navigation/elements/src/Header/Header.tsx
index ff00094..b4e5760 100644
--- a/node_modules/@react-navigation/elements/src/Header/Header.tsx
+++ b/node_modules/@react-navigation/elements/src/Header/Header.tsx
@@ -309,46 +309,56 @@ export function Header(props: Props) {
 
   return (
     <Animated.View
-      pointerEvents="box-none"
-      style={[{ height, minHeight, maxHeight, opacity, transform }]}
+      style={[
+        { height, minHeight, maxHeight, opacity, transform },
+        { pointerEvents: 'box-none' },
+      ]}
     >
       <Animated.View
-        pointerEvents="box-none"
-        style={[StyleSheet.absoluteFill, backgroundContainerStyle]}
+        style={[
+          StyleSheet.absoluteFill,
+          backgroundContainerStyle,
+          { pointerEvents: 'box-none' },
+        ]}
       >
         {headerBackground ? (
           headerBackground({ style: backgroundStyle })
         ) : (
           <HeaderBackground
-            pointerEvents={
-              // Allow touch through the header when background color is transparent
-              headerTransparent &&
-              (backgroundStyle.backgroundColor === 'transparent' ||
-                Color(backgroundStyle.backgroundColor).alpha() === 0)
-                ? 'none'
-                : 'auto'
-            }
-            style={backgroundStyle}
+            style={[
+              backgroundStyle,
+              {
+                pointerEvents:
+                  // Allow touch through the header when background color is transparent
+                  headerTransparent &&
+                  (backgroundStyle.backgroundColor === 'transparent' ||
+                    Color(backgroundStyle.backgroundColor).alpha() === 0)
+                    ? 'none'
+                    : 'auto',
+              },
+            ]}
           />
         )}
       </Animated.View>
-      <View pointerEvents="none" style={{ height: headerStatusBarHeight }} />
       <View
-        pointerEvents="box-none"
+        style={[{ height: headerStatusBarHeight }, { pointerEvents: 'none' }]}
+      />
+      <View
         style={[
           styles.content,
           Platform.OS === 'ios' && frame.width >= IPAD_MINI_MEDIUM_WIDTH
             ? styles.large
             : null,
+          { pointerEvents: 'box-none' },
         ]}
       >
         <Animated.View
-          pointerEvents="box-none"
           style={[
             styles.start,
             !searchBarVisible && headerTitleAlign === 'center' && styles.expand,
             { marginStart: insets.left },
             leftContainerStyle,
+            { pointerEvents: 'box-none' },
           ]}
         >
           {leftButton}
@@ -356,7 +366,6 @@ export function Header(props: Props) {
         {Platform.OS === 'ios' || !searchBarVisible ? (
           <>
             <Animated.View
-              pointerEvents="box-none"
               style={[
                 styles.title,
                 {
@@ -382,6 +391,7 @@ export function Header(props: Props) {
                   ? { marginStart: 4 }
                   : { marginHorizontal: 16 },
                 titleContainerStyle,
+                { pointerEvents: 'box-none' },
               ]}
             >
               {headerTitle({
@@ -393,12 +403,12 @@ export function Header(props: Props) {
               })}
             </Animated.View>
             <Animated.View
-              pointerEvents="box-none"
               style={[
                 styles.end,
                 styles.expand,
                 { marginEnd: insets.right },
                 rightContainerStyle,
+                { pointerEvents: 'box-none' },
               ]}
             >
               {rightButton}
diff --git a/node_modules/@react-navigation/elements/src/Header/HeaderSearchBar.tsx b/node_modules/@react-navigation/elements/src/Header/HeaderSearchBar.tsx
index 9310f86..8610567 100644
--- a/node_modules/@react-navigation/elements/src/Header/HeaderSearchBar.tsx
+++ b/node_modules/@react-navigation/elements/src/Header/HeaderSearchBar.tsx
@@ -157,10 +157,14 @@ function HeaderSearchBarInternal(
 
   return (
     <Animated.View
-      pointerEvents={visible ? 'auto' : 'none'}
       aria-live="polite"
       aria-hidden={!visible}
-      style={[styles.container, { opacity: visibleAnim }, style]}
+      style={[
+        styles.container,
+        { opacity: visibleAnim },
+        style,
+        { pointerEvents: visible ? 'auto' : 'none' },
+      ]}
     >
       <View style={styles.searchbarContainer}>
         <HeaderIcon
diff --git a/node_modules/@react-navigation/elements/src/ResourceSavingView.tsx b/node_modules/@react-navigation/elements/src/ResourceSavingView.tsx
index 63e1dc0..c0b91ef 100644
--- a/node_modules/@react-navigation/elements/src/ResourceSavingView.tsx
+++ b/node_modules/@react-navigation/elements/src/ResourceSavingView.tsx
@@ -30,8 +30,8 @@ export function ResourceSavingView({
           { display: visible ? 'flex' : 'none' },
           styles.container,
           style,
+          { pointerEvents: visible ? 'auto' : 'none' },
         ]}
-        pointerEvents={visible ? 'auto' : 'none'}
         {...rest}
       >
         {children}
@@ -41,9 +41,12 @@ export function ResourceSavingView({
 
   return (
     <View
-      style={[styles.container, style]}
+      style={[
+        styles.container,
+        style,
+        { pointerEvents: visible ? 'auto' : 'none' },
+      ]}
       // box-none doesn't seem to work properly on Android
-      pointerEvents={visible ? 'auto' : 'none'}
     >
       <View
         collapsable={false}
@@ -52,8 +55,10 @@ export function ResourceSavingView({
           // This is an workaround for a bug where the clipped view never re-appears
           Platform.OS === 'ios' || Platform.OS === 'macos' ? !visible : true
         }
-        pointerEvents={visible ? 'auto' : 'none'}
-        style={visible ? styles.attached : styles.detached}
+        style={[
+          visible ? styles.attached : styles.detached,
+          { pointerEvents: visible ? 'auto' : 'none' },
+        ]}
       >
         {children}
       </View>
diff --git a/node_modules/@react-navigation/elements/src/Screen.tsx b/node_modules/@react-navigation/elements/src/Screen.tsx
index 0e2b0b7..b17c8cd 100644
--- a/node_modules/@react-navigation/elements/src/Screen.tsx
+++ b/node_modules/@react-navigation/elements/src/Screen.tsx
@@ -78,13 +78,16 @@ export function Screen(props: Props) {
         <NavigationProvider route={route} navigation={navigation}>
           <View
             ref={headerRef}
-            pointerEvents="box-none"
             onLayout={(e) => {
               const { height } = e.nativeEvent.layout;
 
               setHeaderHeight(height);
             }}
-            style={[styles.header, headerTransparent ? styles.absolute : null]}
+            style={[
+              styles.header,
+              headerTransparent ? styles.absolute : null,
+              { pointerEvents: 'box-none' },
+            ]}
           >
             {header}
           </View>
